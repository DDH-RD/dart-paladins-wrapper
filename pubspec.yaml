name: dart_paladins_wrapper
description: A Paladins Wrapper written in dart.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.1.3

# Add regular dependencies here.
dependencies:
  http: ^1.1.0
  intl: ^0.18.1
  crypto: ^3.0.1
  # path: ^1.8.0

dev_dependencies:
  build_runner: ^2.4.0
  build_web_compilers: ^4.0.0
  lints: ^2.0.0